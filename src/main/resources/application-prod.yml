# Production Configuration for Application

spring:
  datasource:
    url: jdbc:mysql://<RDS_ENDPOINT>:3306/<DB_NAME>
    username: <DB_USERNAME>
    password: <DB_PASSWORD>
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
